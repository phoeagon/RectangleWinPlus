Special instructions:
1. Do not change the `yaml: "modifier"` annotation in `conf.go`. Changing these annotations will break the config parser that loads a file like `config.example.yaml`. `yaml:"modifier"` is NOT correct.
